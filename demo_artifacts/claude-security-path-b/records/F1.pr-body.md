**Closed by Holdfast** (`holdfast close`). Base `holdfast-base-78fea27f69` is the report's stamped revision 78fea27f69. Patch hand-built in the plugin's schema — see PROVENANCE.md.

## Property

In MultiPartParser.parse(), the uploaded file name passed to the upload handler is produced solely by MultiPartParser.sanitize_file_name(), which must perform html.unescape() first, then strip Windows-style backslash paths, and apply os.path.basename() last, so no path-separator characters (real or entity-encoded) can remain in the returned name.

## Evidence by tier, per commit

- **Fix commit** `4ec38c036b` — Directory traversal via HTML-entity-encoded upload file name in MultiPartParser (evidence gathered on the patch applied at 78fea27f69):
  - tier 2 guard_lines: 6/6 guard lines present at fix; 0 of them already present at parent (should be 0).
  - tier 3 removed_lines_absent: 4 removed lines recorded; 0 still present at fix.
  - tier 1 regression_test: not produced: no test changes found in the fix commit to extract from
  - tier 4 property_derivation: model-stated property and sibling judgement (verbatim reply in data)
- **Kept test:** null — patch carries no tests
- **Record commit** `cf657f16a5` — `.holdfast/records/F1.json`

## Completeness

No uncovered consumers found among the 3 examined (protected value: the uploaded file's name taken from the Content-Disposition header in MultiPartParser.parse(); 15 files in context):

- `MultiPartParser.parse` in `django/http/multipartparser.py` — file_name = disposition.get('filename') is passed through force_str then `file_name = self.sanitize_file_name(file_name)` (line 216) before being used in handler.new_file(...) calls, replacing the old basename-before-unescape sequence with unescape->backslash-strip->basename in sanitize_file_name (lines 308-312).
- `FileUploadHandler.new_file / TemporaryFileUploadHandler.new_file / MemoryFileUploadHandler.file_complete` in `django/core/files/uploadhandler.py` — These consume self.file_name, which is set from the already-sanitized file_name produced by MultiPartParser.parse() before new_file() is called (multipartparser.py lines 234-237); no additional path-separator-bearing raw value is introduced here.
- `UploadedFile._set_name` in `django/core/files/uploadedfile.py` — Performs an additional os.path.basename(name) on the name property (line 42), which is redundant defense-in-depth applied after the value has already passed through sanitize_file_name; it does not bypass the fix.

Cannot verify: This contract only verifies the ordering of operations within sanitize_file_name() and its single call site in parse(); it cannot verify that no other code path in Django or downstream upload handlers independently calls os.path.basename() or unescape() on filenames in a different order, nor that all upload handlers correctly trust only the sanitized name. Consumers outside this repository (third-party packages, deployment configuration) are not examined; the consumer list is only as complete as the symbol references the query was shown.

Derived findings: none. Derived findings, when listed, go back through Suggest patches; no sibling patches are in this PR.

---
Nothing in this PR was applied without the user choosing close.
