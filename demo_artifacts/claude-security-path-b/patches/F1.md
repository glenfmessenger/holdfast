# F1 — patch note (HAND-BUILT)

This patch is Django's fix d4d800ca1a for CVE-2021-28658 (multipartparser.py part only), placed here so Holdfast can be demonstrated on a report in the plugin's schema. No patch-generator or patch-verifier agent ran. See ../PROVENANCE.md.
