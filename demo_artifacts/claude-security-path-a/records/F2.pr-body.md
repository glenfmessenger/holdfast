**Closed by Holdfast** (`holdfast close`). Base `holdfast-base-78fea27f69` is the report's stamped revision 78fea27f69. Patch produced by Claude Security plugin.

## Property

In MultiPartParser.parse, the filename derived from Content-Disposition must have HTML entities decoded (html.unescape) before any path-separator stripping occurs, and the resulting sanitize_file_name() output must reject '', '.', and '..' outright — sanitize_file_name(file_name) is the sole gate between the raw header value and file_name used by upload handlers.

## Evidence by tier, per commit

- **Fix commit** `447bbef7b6` — Close F2: Multipart filename sanitized before HTML-unescape, allowing path separators to be reintroduced (evidence gathered on scratch commit cadf330d0f (the patch applied at 78fea27f69); branch commit 447bbef7b6):
  - tier 2 guard_lines: 16/16 guard lines present at fix; 1 of them already present at parent (should be 0).
  - tier 3 removed_lines_absent: 5 removed lines recorded; 0 still present at fix.
  - tier 1 regression_test: extracted test(s) ['file_uploads.tests.FileUploadTests.test_blank_filenames', 'file_uploads.tests.FileUploadTests.test_dangerous_file_names', 'file_uploads.tests.MultiParserTests.test_sanitize_file_name', 'file_uploads.tests.MultiParserTests.test_sanitize_invalid_file_name', 'file_uploads.tests.MultiParserTests.test_upload_handler_receives_sanitized_file_name']: FAIL on parent 78fea27f69 (FAILED (failures=10, errors=27)), PASS on scratch commit cadf330d0f (the patch applied at 78fea27f69); branch commit 447bbef7b6.
  - tier 4 property_derivation: model-stated property and sibling judgement (verbatim reply in data)
- **Kept test:** commit `7ca5df62a2`, paths tests/file_uploads/tests.py
- **Record commit** `9ea2628b0c` — `.holdfast/records/F2.json`

## Completeness

No uncovered consumers at medium or high confidence among the 3 examined; 1 listed at low confidence (see derived findings) (protected value: the client-supplied upload filename parsed from the Content-Disposition header in MultiPartParser.parse (file_name); 25 files in context):

- `MultiPartParser.parse` in `django/http/multipartparser.py` — The sole point where the raw Content-Disposition filename is converted: 'file_name = force_str(file_name, encoding, errors='replace'); file_name = self.sanitize_file_name(file_name)' at lines 214-215, replacing the old os.path.basename()+IE_sanitize(html.unescape()) sequence. sanitize_file_name() now calls html.unescape() before rsplit('/') and rsplit('\\'), and rejects '', '.', '..'.
- `FileUploadHandler.new_file / TemporaryFileUploadHandler.new_file / MemoryFileUploadHandler.file_complete` in `django/core/files/uploadhandler.py` — These consumers (new_file, TemporaryUploadedFile, InMemoryUploadedFile) all receive file_name only after MultiPartParser.parse has already called sanitize_file_name(), e.g. 'handler.new_file(field_name, file_name, content_type, ...)' at line 234, using the already-sanitized file_name from line 215.
- `_set_name` in `django/core/files/uploadedfile.py` — listed at low confidence: This setter still runs os.path.basename() on the name assigned to UploadedFile.name, independent of the new sanitize_file_name gate. It's not in the scope of the F2 fix and doesn't unescape HTML entities, but since new_file() in TemporaryFileUploadHandler/InMemoryUploadedFile constructs UploadedFile with self.file_name (already sanitized), this consumer receives an already-sanitized value in the normal flow. However, if any other caller passes an unsanitized, HTML-encoded name directly to UploadedFile(name=...), basename() would run pre-unescape here, but this is outside the file_name flow controlled by the guard and not shown to be reachable with raw header data in the excerpts.

Cannot verify: This contract only verifies the ordering and rejection logic within sanitize_file_name/parse in multipartparser.py; it cannot verify that all downstream consumers (Storage backends, custom upload handlers) properly reject or re-sanitize edge cases like drive-letter prefixes (e.g. 'D:foo'), which the patch notes were already reachable and not addressed. It also cannot verify behavior for encodings or separator characters not covered by the '/' and '\' rsplit logic (e.g. other OS-specific separators) beyond what the test suite exercised. Consumers outside this repository (third-party packages, deployment configuration) are not examined; the consumer list is only as complete as the symbol references the query was shown.

Derived findings: F2.1 (1 listed at low confidence, below the verdict threshold). Derived findings go back through Suggest patches; no sibling patches are in this PR.

---
Nothing in this PR was applied without the user choosing close.
