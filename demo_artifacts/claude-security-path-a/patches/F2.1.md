# F2.1 — derived by Holdfast from F2

**Consumer:** `_set_name` in `django/core/files/uploadedfile.py`, line 0.

**Line(s) that consume the protected value:**

```
name = os.path.basename(name)
```

**Why it is not covered by F2's patch:** This setter still runs os.path.basename() on the name assigned to UploadedFile.name, independent of the new sanitize_file_name gate. It's not in the scope of the F2 fix and doesn't unescape HTML entities, but since new_file() in TemporaryFileUploadHandler/InMemoryUploadedFile constructs UploadedFile with self.file_name (already sanitized), this consumer receives an already-sanitized value in the normal flow. However, if any other caller passes an unsanitized, HTML-encoded name directly to UploadedFile(name=...), basename() would run pre-unescape here, but this is outside the file_name flow controlled by the guard and not shown to be reachable with raw header data in the excerpts.

**Confidence:** low (the completeness query's own confidence for this consumer).

**Protected value:** the client-supplied upload filename parsed from the Content-Disposition header in MultiPartParser.parse (file_name)

No patch is written for a derived finding. Run Suggest patches on `F2.1` to get one; it will be built and reviewed like any other patch.
